// commitlint.config.js
module.exports = {
    extends: ['@commitlint/config-conventional'],
    rules: {
      // Define custom rules or use default ones here
      'subject-case': [2, 'always', 'sentence-case'],
      // Add more rules as needed
    },
  };
  